{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "@powershell Remove-Item -Recurse -Force build && cd ../phonebook-frontend && npm run build & @powershell Copy-Item dist -Recurse ../phonebook-backend",    "deploy:fill": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.2",
    "nodemon": "^3.1.0",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "morgan": "^1.10.0"
  }
}
